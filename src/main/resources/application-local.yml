spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sellerbot
    username: sellerbot_user
    password: sellerbot_pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: false
  flyway:
    enabled: true
openai:
  token: самый-секретный-токен-из-openai
